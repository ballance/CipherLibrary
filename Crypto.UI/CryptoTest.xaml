<UserControl x:Class="Crypto.UI.CryptoTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Crypto.UI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Background="Aqua">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <Label Content="Input"   />
            <TextBox Name="InputBox" Height="100" />
            <StackPanel Orientation="Horizontal">
            <Button Name="GoButton" Content="Go" Click="GoButton_OnClick"/>
            <Button Name="DecryptCheck" Content="Decrypt &amp; Check" />
            </StackPanel>
            <Label Content="Output" />
            <TextBox Name="OutputBox2" Height="100" />
        </StackPanel>
    </Grid>
</UserControl>
